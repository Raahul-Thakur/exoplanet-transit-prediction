[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "exoplanet-transit"
version = "0.1.0"
description = "Modular exoplanet transit detection and observation planning framework"
authors = [{ name = "Rahul Thakur" }]
dependencies = []
